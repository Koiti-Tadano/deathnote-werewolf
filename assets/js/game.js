// === game.js 開始 ===
import { db, ref } from "./firebase.js";

export async function assignRolesAndProfiles(roomId) {
  // 今の assignRolesAndProfiles 関数をここに移動
}

export async function startPhaseInDB(phase, day, durationSec) {
  // startPhaseInDB 関数
}

export async function nextPhaseInDB(phase, day) {
  // nextPhaseInDB 関数
}
// === game.js 終了 ===
